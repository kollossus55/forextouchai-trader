{
  "name": "BrokerConnection",
  "type": "object",
  "properties": {
    "platform": {
      "type": "string",
      "enum": [
        "MT4",
        "MT5"
      ],
      "default": "MT4"
    },
    "server_name": {
      "type": "string",
      "description": "Broker server name"
    },
    "account_number": {
      "type": "string"
    },
    "password": {
      "type": "string",
      "description": "Trading account password (stored securely)"
    },
    "api_key": {
      "type": "string",
      "description": "Optional: Bridge provider API Key for added security"
    },
    "connection_status": {
      "type": "string",
      "enum": [
        "CONNECTED",
        "DISCONNECTED",
        "ERROR"
      ],
      "default": "DISCONNECTED"
    },
    "last_sync": {
      "type": "string",
      "format": "date-time"
    },
    "balance": {
      "type": "number",
      "default": 0
    },
    "equity": {
      "type": "number",
      "default": 0
    },
    "margin": {
      "type": "number",
      "default": 0
    },
    "free_margin": {
      "type": "number",
      "default": 0
    },
    "margin_level": {
      "type": "number",
      "default": 0
    },
    "leverage": {
      "type": "string",
      "default": "1:100"
    },
    "currency": {
      "type": "string",
      "default": "USD"
    },
    "owner_email": {
      "type": "string",
      "description": "Email of the user who owns this broker connection"
    },
    "open_trade_count": {
      "type": "number",
      "default": 0,
      "description": "Number of currently open trades on this account (synced by bridge)"
    }
  },
  "required": [
    "server_name",
    "account_number"
  ],
  "rls": {
    "create": {},
    "read": {
      "$or": [
        {
          "owner_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "update": {
      "$or": [
        {
          "owner_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    },
    "delete": {
      "$or": [
        {
          "owner_email": "{{user.email}}"
        },
        {
          "created_by": "{{user.email}}"
        },
        {
          "user_condition": {
            "role": "admin"
          }
        }
      ]
    }
  }
}